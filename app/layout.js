import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Nave Support - Digital Growth Partner",
  description: "Butuh bantuan atau ingin berkonsultasi? Nave Support siap membantu Anda mendapatkan informasi mengenai layanan Website Development, Digital Marketing, SEO Optimization, dan Chatbot Automation sesuai kebutuhan bisnis Anda.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
